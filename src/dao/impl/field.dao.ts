import {BaseDao} from "../base.dao";
import FieldModel from "../../model/field.model";

class FieldDao implements BaseDao<FieldModel>{
    create(dataObj: FieldModel) {
    }

    delete(id: string) {
    }

    findAll() {
    }

    update(dataObj: FieldModel) {
    }

}